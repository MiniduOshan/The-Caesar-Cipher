// Main program for Caesar Cipher using Circular Queue

#include <stdio.h>
#include <string.h>
#include "cipher.h"

int main() {
    char choice;           // User's choice: E for encryption, D for decryption
    char message[1000];    // Input message from the user
    char result[1000];     // Result after encryption/decryption
    int shift;             // Shift key value
    char repeat;           // User's choice to repeat the process
}